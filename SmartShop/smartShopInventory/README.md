python3Code

将算法检查识别结果标注到图片上，使得检测效果可视化，辅助测试

1、smartShopInventory.py

功能：调用理货算法接口，将返回的商品信息（sku编号和坐标）保存文件，且将商品的坐标和名称画到图片上

2、drawROI.py（定义被调用函数，其他画框需求也可满足）

功能：图片画框后保存

传参：需传入图片位置、图片中目标坐标及名称、画框后图片保存路径（可选参数）
